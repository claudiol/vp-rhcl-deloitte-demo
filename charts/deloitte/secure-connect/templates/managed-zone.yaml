---
apiVersion: kuadrant.io/v1alpha1
kind: ManagedZone
metadata:
  name: managedzone
  namespace: {{ .Values.secure.config.gatewayNS }}
spec:
  id: {{ .Values.secure.config.hostedZoneID }}
  domainName: {{ .Values.secure.config.rootDomainName }}
  description: "Kuadrant managed zone"
  dnsProviderSecretRef:
    name: aws-credentials
